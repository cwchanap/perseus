// Global Vitest setup for @perseus/api. Suite-local concerns (e.g. deterministic
// Math.random pinning for retry-budget tests) live in their respective test
// files' beforeEach/afterEach hooks rather than here.
